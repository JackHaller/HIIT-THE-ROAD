using UnityEngine;
using System.Collections;

//Adjusts the texture scaling of an object based on it's size (currently only works for sandpits because it goes off the parent's size)
public class AutoTileTexture : MonoBehaviour {

	//public float scalingFactor = 0.5f;
	//public bool useParentScale = true;
	
 //   // Use this for initialization
 //   void Start () {
	//	Transform transformTouse = useParentScale ? this.gameObject.transform.parent : this.transform;
	//	Vector2 scale = new Vector2(transformTouse.localScale.x, transformTouse.localScale.z) * scalingFactor;
	//	gameObject.GetComponent<Renderer>().material.mainTextureScale = scale;
	//}
}
